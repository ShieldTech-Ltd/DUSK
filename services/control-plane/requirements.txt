# Runtime dependencies used by the dependency-audit control.
# Keep these constraints synchronized with pyproject.toml.
fastapi>=0.116,<1
httpx2>=2.12,<3
PyJWT[crypto]>=2.13,<3
pydantic-settings>=2.10,<3
uvicorn>=0.35,<1
