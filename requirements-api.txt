# Flask API server
flask
flask-cors
requests
python-dotenv

# Partner integrations
tavily-python
google-generativeai
# mubit -- not on PyPI; install via hackathon access: pip install mubit

# Aikido runtime security -- not on PyPI; install via aikido.dev
# aikido-firewall

# HTTP client (already in stdlib but explicit for clarity)
urllib3
